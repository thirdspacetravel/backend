enum Gender {
  MALE
  FEMALE
  OTHER
}

enum MaritalStatus {
  SINGLE
  MARRIED
  DIVORCED
  WIDOWED
  PARTNERED
}

enum ContactMethod {
  WHATSAPP
  EMAIL
  PHONE
}

enum AdminRole {
  SUPER_ADMIN
  TRIP_MANAGER
}

enum AccountStatus {
  PENDING_VERIFICATION
  VERIFIED
  SUSPENDED
  DELETED
}

enum TripStatus {
  DRAFT
  PUBLISHED
  COMPLETED
  CANCELLED
}

enum TripCategory {
  WINTER_EXPEDITION @map("WINTER EXPEDITION")
  SUMMER_SPECIAL    @map("SUMMER SPECIAL")
  TREKKING          @map("TREKKING")
  BIKING_TRIP       @map("BIKING TRIP")
  BACKPACKING       @map("BACKPACKING")
  WEEKEND_GETAWAY   @map("WEEKEND GETAWAY")
}
