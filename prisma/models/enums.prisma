enum Gender {
  MALE
  FEMALE
  OTHER
}

enum MaritalStatus {
  SINGLE
  MARRIED
  DIVORCED
  WIDOWED
  PARTNERED
}

enum ContactMethod {
  WHATSAPP
  EMAIL
  PHONE
}

enum AdminRole {
  SUPER_ADMIN
  TRIP_MANAGER
}

enum AccountStatus {
  PENDING_VERIFICATION
  VERIFIED
  SUSPENDED
  DELETED
}
